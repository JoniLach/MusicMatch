<Page x:Class="MusicMatch.BookLessonPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicMatch"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Book Lesson" Background="#121212">

    <Page.Resources>
        <local:StatusColorConverter x:Key="StatusColorConverter"/>
        <local:StatusTextConverter x:Key="StatusTextConverter"/>
        
        <Style x:Key="BookButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="#1DB954"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Content" Value="Book Now"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="17.5" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#FF5555"/>
                                <Setter Property="Foreground" Value="#DDDDDD"/>
                            </Trigger>
                             <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1ED760"/>
                            </Trigger>
                            <!-- Custom check for IsBooked is hard in pure XAML without DataTrigger binding to the context -->
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                 <DataTrigger Binding="{Binding IsBooked}" Value="True">
                    <Setter Property="IsEnabled" Value="False"/>
                    <Setter Property="Content" Value="BOOKED"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/> <!-- Expanded sidebar -->
            <ColumnDefinition Width="*"/>    <!-- Slots -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.ColumnSpan="2" Text="Book a Lesson" FontSize="28" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,20"/>

        <!-- Calendar Section -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Border Background="#1E1E1E" CornerRadius="10" Padding="10" VerticalAlignment="Top">
                <Calendar x:Name="calDate" SelectedDatesChanged="calDate_SelectedDatesChanged" Background="Transparent" BorderThickness="0" Foreground="White"/>
            </Border>

            <!-- My Booked Sessions Section -->
            <TextBlock Grid.Row="1" Text="Quick Select" Foreground="White" FontWeight="SemiBold" Margin="0,20,0,10"/>
            
            <ListBox x:Name="lstAvailableDates" Grid.Row="2" Background="Transparent" BorderThickness="0" SelectionChanged="lstAvailableDates_SelectionChanged" MaxHeight="150" Margin="0,0,0,20">
                 <!-- Style remains same -->
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                         <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="border" CornerRadius="5" Padding="10,5" Margin="0,0,0,5" Background="#252525" Cursor="Hand">
                                        <Grid>
                                             <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Date, StringFormat=d}" Foreground="#DDDDDD"/>
                                            <Border Grid.Column="1" Background="#1DB954" CornerRadius="10" Padding="8,2" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding Count}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#333333"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="border" Property="Background" Value="#1DB954"/>
                                            <Setter TargetName="border" Property="BorderBrush" Value="#1DB954"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <TextBlock Grid.Row="3" Text="My Upcoming Sessions" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <ListBox x:Name="lstMySessions" Grid.Row="4" Background="Transparent" BorderThickness="0" MaxHeight="200">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border CornerRadius="5" Padding="10" Margin="0,0,0,5" Background="#333333">
                                        <StackPanel>
                                            <TextBlock Text="{Binding LessonDate, StringFormat=d}" Foreground="White" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding StartTime}" Foreground="#AAAAAA"/>
                                        </StackPanel>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>

        <!-- Available Slots Section -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="txtSelectedDate" Text="Select a date to see available slots" Foreground="#AAAAAA" FontSize="16" Margin="0,0,0,10"/>

            <ListView x:Name="lstSlots" Grid.Row="1" Background="Transparent" BorderThickness="0">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Margin" Value="0,0,0,10"/>
                        <Setter Property="Focusable" Value="False"/> 
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border Background="#1E1E1E" CornerRadius="8" Padding="15">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding StartTime}" Foreground="White" FontSize="18"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Duration}" Foreground="White"/>
                                    <TextBlock Text=" mins" Foreground="White"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <Button Grid.Column="1" Click="btnBook_Click" Tag="{Binding Id}" Style="{StaticResource BookButtonStyle}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            <TextBlock x:Name="txtNoSlots" Text="No slots found for this date." Visibility="Collapsed" Foreground="#666666" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Grid.Row="1"/>
        </Grid>

        <!-- Footer -->
        <Button Grid.Row="2" Grid.ColumnSpan="2" x:Name="btnBack" Content="Back" Click="btnBack_Click" Width="100" Height="35" HorizontalAlignment="Left" Margin="0,20,0,0" Background="#333333" Foreground="White" BorderThickness="0">
             <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Page>
